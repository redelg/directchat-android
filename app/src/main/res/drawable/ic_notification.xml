<?xml version="1.0" encoding="utf-8"?>
<!--
  Mensaji notification icon — 24dp, single-color (white), transparent background.
  At this scale the connector reads as a smudge, so it's omitted. Sender is a
  filled disc; receiver is a ring — gives the two nodes distinct silhouettes
  even at 24×24 px in a status bar.

  Drop in: app/src/main/res/drawable/ic_notification.xml
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="?attr/colorControlNormal">

    <!-- sender: solid disc r=4 @ (8,12) -->
    <path
        android:pathData="M 4,12 a 4,4 0 1,0 8,0 a 4,4 0 1,0 -8,0 Z"
        android:fillColor="#FFFFFF" />

    <!-- receiver: ring r=4 @ (16,12), stroke 1.5 -->
    <path
        android:pathData="M 12,12 a 4,4 0 1,0 8,0 a 4,4 0 1,0 -8,0 Z"
        android:fillColor="#00000000"
        android:strokeColor="#FFFFFF"
        android:strokeWidth="1.5" />
</vector>
